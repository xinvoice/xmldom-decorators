{
  "name": "root",
  "private": true,
  "dependencies": {
    "lerna": "^8.0.2"
  },
  "scripts": {
    "build": "lerna run build",
    "build:ci": "lerna run build --scope=@xinvoice/xmldom-*",
    "test": "lerna run test"
  },
  "version": "1.0.5",
  "workspaces": [
    "packages/xmldom-decorators-cli",
    "packages/xmldom-decorators-test",
    "packages/xmldom-decorators",
    "packages/sample-clients"
  ]
}
